---
layout: null
permalink: /feed.xml
---
{%- comment -%}
  Custom Atom feed that mirrors what jekyll-feed produces but filters out
  posts marked `hidden: true` in their front matter. Authoring our own
  `feed.xml` causes jekyll-feed to skip its auto-generated version.
{%- endcomment -%}
{%- assign visible_posts = site.posts | where_exp: "p", "p.hidden != true" -%}
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" />
  {%- if visible_posts.first.date -%}
  <updated>{{ visible_posts.first.date | date_to_xmlschema }}</updated>
  {%- else -%}
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  {%- endif -%}
  <id>{{ '/feed.xml' | absolute_url | xml_escape }}</id>
  <title type="html">{{ site.title | smartify | xml_escape }}</title>
  {%- if site.description -%}
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  {%- endif -%}
  {%- if site.author -%}
  <author>
    <name>{{ site.author.name | default: site.author | xml_escape }}</name>
    {%- if site.author.email -%}
    <email>{{ site.author.email | xml_escape }}</email>
    {%- endif -%}
  </author>
  {%- endif -%}
  {%- for post in visible_posts limit: 30 -%}
  <entry{%- if post.lang %} xml:lang="{{ post.lang }}"{%- endif %}>
    <title type="html">{{ post.title | smartify | strip_html | normalize_whitespace | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.id | absolute_url | xml_escape }}</id>
    <content type="html" xml:base="{{ post.url | absolute_url | xml_escape }}">{{ post.content | strip | xml_escape }}</content>
    {%- if site.author -%}
    <author>
      <name>{{ site.author.name | default: site.author | xml_escape }}</name>
      {%- if site.author.email -%}
      <email>{{ site.author.email | xml_escape }}</email>
      {%- endif -%}
    </author>
    {%- endif -%}
    {%- for cat in post.categories -%}
    <category term="{{ cat | xml_escape }}" />
    {%- endfor -%}
    {%- for tag in post.tags -%}
    <category term="{{ tag | xml_escape }}" />
    {%- endfor -%}
    {%- if post.excerpt -%}
    <summary type="html">{{ post.excerpt | strip_html | normalize_whitespace | xml_escape }}</summary>
    {%- endif -%}
  </entry>
  {%- endfor -%}
</feed>
